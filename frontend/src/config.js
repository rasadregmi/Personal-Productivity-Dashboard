export const API_BASE_URL = 'http://localhost:4000/api';

export const config = {
  apiBaseUrl: API_BASE_URL,
  backend: {
    host: 'localhost',
    port: 4000,
    protocol: 'http'
  },
  frontend: {
    host: 'localhost', 
    port: 3000,
    protocol: 'http'
  }
};

export default config;